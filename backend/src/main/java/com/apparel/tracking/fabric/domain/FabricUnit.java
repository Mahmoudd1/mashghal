package com.apparel.tracking.fabric.domain;

/** How a fabric type is measured. Fixed per type and shared by all its rolls. */
public enum FabricUnit {
    /** Kilograms. */
    KG,
    /** Metres. */
    LENGTH
}
